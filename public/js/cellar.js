$(document).ready(() => {
    const add = $('button#add');
    const remove = $('button#remove');
    const wineid = $('');
    const userid = $('');

    add.on("click", event => {
        event.preventDefault();
        
    });

    remove.on("click", event => {
        event.preventDefault();
        
    })
});